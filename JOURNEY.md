# 이 프로젝트에서 무엇을 고민했고 어디서 틀렸나

이건모 · 2026-08-27 ~ 09-15 · 맥북 → SSAFY 노트북

성과 기록은 [README.md](README.md) 에 있다. 이 문서는 **틀린 것과 그 이유**를 적는다.
같은 실수를 되풀이하지 않으려는 것이고, 남이 이어받을 때 이미 막힌 길을 다시 가지 않게 하려는 것이다.

---

## 0. 가장 크게 배운 것 하나

> **정보가 늘어도 1등 고르기는 안 움직인다.**

이 프로젝트에서 **여섯 번** 같은 일이 있었다:

| 무엇을 넣었나 | 확률 품질(logloss) | 1착 적중(top-1) |
|---|---|---|
| 마체중 | 유의하게 개선 (LR 15.79, p=7.1e-05) | −0.26%p [−0.88, +0.36] |
| Benter 2단계 결합 | 개선 | 움직임 없음 |
| S3 말별 GRU | 일관되게 개선 | LightGBM 못 넘음 |
| H1 착차 피처 | 개선 (p=7.6e-18) | 상한 +0.6%p |
| H2 경쟁강도 | 개선 | 상한 +0.6%p |
| 그룹 내 지표 비중 적합 | −0.0167 | +0.6%p (표준오차 ±1.27%p) |

처음 두세 번은 "다음엔 되겠지" 했다. 여섯 번째쯤 되니 이건 **우연이 아니라 구조**다.
말 열 마리 중 1등을 맞히는 건 **경계 근처의 몇 마리를 가르는 일**인데,
대부분의 새 정보는 **이미 확실한 말을 더 확실하게** 만들 뿐이다.

그래서 지금은 **top-1 이 안 움직이면 "효과 없음"이라고 쓴다.** logloss만 좋아진 걸
성과로 포장하지 않는다.

---

## 1. 규칙을 먼저 정한 이유

### 검출 한계를 계산하고 시작했다

`valid` 1,254경주에서 top-1 표준오차는 **±0.85%p**. 두 모델을 비교하면 짝 상관을 감안해도
**±2.39%p** 아래는 구별할 수 없다. 이걸 먼저 계산해 두니 많은 것이 정리됐다:

- "33.1% vs 33.5%" 같은 표를 만들고 고민하지 않게 됐다
- 시드를 3개 이상 돌려 **평균±표준편차**로 읽게 됐다
- 하루 15경주 실전 성적(±12%p)으로 모델을 고치지 않게 됐다

### 건드리면 안 되는 것을 못박았다

```
game 3,700경주   학습·검증·튜닝 어디에도 쓰지 않는다 (제품 리플레이 풀이다)
test             열지 않는다. 최종 1회
비교·튜닝        valid 에서만
정렬             race_id 연속 블록을 바꾸지 않는다 (LightGBM group 이 그걸 가정한다)
채점             팀 채점기 docs/model/common.py 를 반드시 거친다
```

**이 중 `valid` 규칙이 나중에 문제가 됐다** — §5 에 적는다.

---

## 2. 논쟁 대신 계측한 것 셋

말로 정할 수 있을 것 같은 문제가 사실은 **재면 끝나는 문제**였던 경우가 세 번 있었다.

### 2-1. 마체중을 실시간에 쓸 수 있나

"발주 전에 들어온다 / 안 들어온다"를 추측할 수 있었지만, 틀리면 **실시간 모델에 미래가 샌다.**
그래서 경마 시행일에 API를 반복 호출해 **채워지는 순간을 관측**했다.

```
T−65분  비어 있음
T−61분  채워짐          → tier = "B"
```

### 2-2. 실시간 배당을 구할 수 있나

이건 오래 끌었다. 결국 전수 조사했다:

```
오픈 API 배당 5종        전부 '시행된 경주의 확정배당'. 내일 조회 0행
race.kra.co.kr 경로 73개 / m.kra.co.kr 65개    배당 페이지 없음
m.kra.co.kr JS 번들 9개  배당 AJAX 엔드포인트 0개
시행일 실측 1,168회 관측  발주 T−5 ~ +1분에만 존재
```

정원님도 독립적으로 같은 결론에 닿았고(`selfsup/ledger.md` P13), 가짜 배당을 학습시켜
흉내 내는 시도까지 −1.75 로 실패했다. 도연님 모델엔 배당 축이 아예 없다.
**세 사람이 따로 확인하고 같은 답이 나온 건 이것 하나뿐이다.**

### 2-3. 온도 상수가 0.0445 인가 0.1 인가

명세서는 `0.0445`, 백엔드 구현은 `exp(s/10)` = `0.1` 이었다.
**어느 쪽이 맞나를 따지다가, 문제가 그게 아니라는 걸 알았다.**

스키마의 한 줄 — *"0~100. 경주 내 순위 기반 환산"* — 이 **세 가지로 읽혔고**,
읽기마다 최적 T가 달랐다:

| 읽기 | 최적 T |
|---|---:|
| 지표 순위 → 그룹 평균 | 0.069 |
| 그룹 평균 → 축을 다시 순위 환산 | 0.041 |
| 그룹 평균 → 0~100 재척도 | 0.037 |

**숫자 싸움이 아니라 정의를 안 정한 문제였다.** 축을 순위 환산하기로 못박으니
생성기를 다섯 가지로 바꿔도 T가 0.0406~0.0478 안에 머물렀고, 명세값 0.0445 가 맞았다.
그리고 타워 내부의 `T=1.436`(z척도)과 `0.0445`(0~100척도)가 **같은 온도**라는 것도 알았다 —
`1.436 / 28.7 ≈ 0.050`. `0.1` 만 어느 자에도 안 맞았다.

---

## 3. 틀린 것들

### 3-1. 데이터를 잘못 읽은 것

**`diffUnit` 을 누적 착차로 봤다.** 실제로는 **직전 말과의 간격**이었다.
cumsum 해야 우승마 기준 마신차가 된다. 확인은 `rcTime` 과의 상관으로 했다 — r=0.9932,
초당 약 5.67마신. 이 검증을 안 했으면 H1 피처 전체가 조용히 틀렸을 것이다.

**`diffUnit=500` 을 실제 값으로 봤다.** 센티널이었다. 그 행들의 실제 시간차가 32.6초
(약 185마신)다. 99.5 백분위(41.75)에서 윈저화했다 — 0.497% 잘렸다.

**`ord ≥ 91` 을 착순으로 봤다.** 91~95·98·99는 미출주·취소·실격이다. 원장의 2.22%.

**`race_id` 키를 문자열로 만들었다.** 빌더는 `meet` 을 숫자 코드로 쓰는데 나는
`"부산경남"` 같은 원문을 썼다. **조인 교집합이 0이었다.** 고친 뒤 100.00%.
"조인 결과가 0행"은 다행히 눈에 띄는 실패였다 — 안 띄었으면 더 나빴다.

### 3-2. 누수 — 제일 무서웠던 것

`build_v2.rolling` 이 **무조건** 누적값을 갱신한다. 그런데 기수·조교사·부마는
**같은 날 여러 경주에 나온다.** 그래서 3R 의 기수 승률이 1R·2R 결과를 이미 먹고 있었다.

**가짜 착순을 넣고 값이 반응하는지 보는 방식으로 잡았다.** 9개 컬럼이 반응했고 149행 중
106행이 오염돼 있었다. 첫 등장값을 하루 내내 전파하도록 고쳤다.

그런데 이 검사에는 **구멍이 있다** — 분모만 늘어나는 컬럼(`F2_sire_prog_n` 같은)은
착순을 바꿔도 반응하지 않는다. 그래서 검사에 안 걸린다. **검사를 통과했다는 게
안전하다는 뜻이 아니다.** 그것들도 같이 고쳤다.

### 3-3. 비교 자체를 망친 것

**XGBoost 를 불리하게 돌렸다.** LightGBM 의 `min_data_in_leaf=200` 을 그대로
`min_child_weight=200` 으로 옮겼는데, 후자는 **행 수가 아니라 헤시안 합**이다.
훨씬 강한 제약이었다. 고쳐서 다시 쟀더니 — **결과가 안 바뀌었다.** 계열 자체가
한계에 닿아 있었다.

**raw logloss 로 계열을 비교했다.** `lgb_binary` 가 2.2511(거의 무작위)로 나와서
"나쁜 모델"인 줄 알았는데, **점수 척도가 달랐을 뿐**이다. 확률을 그대로 내놓는 계열은
softmax 가 한 번 더 걸려 균등해진다. 온도 보정(2겹 교차적합)을 넣으니 1.9542 였다.
**모델이 나쁜 게 아니라 자가 틀렸다.**

**도달할 수 없는 가중치로 검증했다.** 왕복 검증을 정확 균등(16.67씩)으로 쟀더니
파일과 메모리가 0.6%p 어긋났다. 그런데 슬라이더는 **정수·합 100** 이라 16.67 을
만들 수 없다. 그 지점에서만 1위 동점이 3.3% 경주로 튄다. 도달 가능한 기본형
(17·17·12·22·19·13)으로 재니 **정확히 일치**했다.

### 3-4. 최적화가 아니었던 것

그룹 내 지표 비중을 적합했는데 결과가 **균등값과 소수 둘째 자리까지 같았다.**
"차이가 없구나" 하고 넘어갈 뻔했는데, 너무 똑같아서 이상했다.

scipy Nelder-Mead 는 초기점이 0 이면 초기 심플렉스를 **0.00025** 로만 민다.
14차원에서 사실상 제자리다. **최적화가 아니라 버그였다.**
초기 심플렉스를 직접 주니 그제서야 움직였다(train nll 2.16 → 2.14).

> 교훈 — **"차이 없음"이 나왔을 때 그게 진짜 차이 없음인지 코드가 안 돈 건지 구분해야 한다.**
> 적합 전후 목적함수 값을 같이 찍는 습관을 들였다.

### 3-5. 운영 사고

9/11 에 배당 관측기를 `--watch` 로 켜 두고 **회수하지 않았다.** 사흘간 돌면서
API 할당량을 태웠고(HTTP 429), 기록에 T−4,429분짜리 관측이 남았다. 관측 수가
1,168 → 6,136 으로 불었다.

원인은 하나였다 — **`--watch` 에 끝이 없었다.** 마지막 발주 +20분, 또는 2,000콜에서
멈추도록 박았다. 발주 시각을 못 읽으면 반복 자체를 안 한다.

그리고 실시간 예측 중에 **제주 6R T−12분 창을 놓쳤다.** 갱신 기능을 구현하느라
시간을 쓰다가 T−1분에 예측이 나갔다. 그건 사전 예측이 아니라서 기록에 그렇게 남겼다.

### 3-6. 환경

- **LightGBM 이 한글 경로에 모델을 저장 못 한다** (Windows C++ 계층). `model_to_string()` 으로 우회
- **cp949 콘솔에서 한글이 깨진다.** `PYTHONUTF8=1`
- **heredoc 안에서 `\n` 이 실제 줄바꿈이 됐다.** 소스 파일이 세 번 깨졌다. 따옴표 heredoc 과 줄 번호 기반 편집으로 바꿨다
- **CSV append 에 컬럼을 2개 더했다.** 헤더는 17칸인데 19칸을 쓰니 `Expected 17 fields, saw 19` 로 자동 루프가 죽었다. 합집합 컬럼으로 자가 복구하게 고쳤다
- **T−minus 를 HH:MM 만으로 계산했다.** 하루 전에 돌리니 전부 "지남"으로 나왔다. 날짜를 포함시켰다

### 3-7. 확인하지 않고 말한 것

인계 문서가 없다고 단정했는데, **내 `origin/main` 이 낡아 있었고 fetch 를 안 했다.**
푸시가 거부되고 나서야 알았다. 파일이 없다고 말하기 전에 원격을 당겨봐야 한다.

---

## 4. 막다른 길 — 다시 가지 말 것

| 시도 | 결과 |
|---|---|
| 2004~2009 원장 백필 (+41% 데이터) | McNemar p=0.868 · ANOVA 데이터셋 기여 **0.0%** |
| "비 오면 가벼운 말이 유리" | 게임풀 3,063경주에서 기각. 차이의차이 +1.07%p [−0.62, +2.76] |
| 모델 계열 16종 탐색 | 전부 **29.4~34.6%** 안. 유의한 차이 둘뿐이고 둘 다 더 나쁨. 예측 상관 중앙값 0.94 |
| PCA / 피처 회전 | 순수 회전인데도 손해 |
| 스태킹 메타모델 (정원) | 단순 평균 대비 −0.96 [−2.23, +0.29] |
| 가짜 배당 증류 (정원) | −1.75 |

**계열을 바꾸는 것으로는 안 된다**는 게 16종 실험의 결론이다.
천장은 모델이 아니라 **입력 정보량**에 있다.

---

## 5. 판단이 바뀐 지점

### valid 를 너무 많이 썼다

규칙상 "비교·튜닝은 valid 에서"였는데, 그 말대로 하다 보니 **같은 1,254경주를 수십 번
들여다봤다.** 거기서 고른 선택이 진짜 일반화인지, valid 에 맞춘 건지 구분이 안 되기 시작했다.

그래서 **`train` 내부 시계열 CV** (확장 윈도, 5겹, 평가 27,964경주)로 옮겼다.
`valid`·`test` 를 안 건드리면서 검출력은 22배가 됐다. H1+H2 확정은 여기서 했다.

**한 번 더 같은 교훈이 있었다** — 역배형의 시장 계수 `m` 을 train 에서 맞췄더니
−0.50, 평균 배당 25.7배라는 인상적인 값이 나왔다. **전이되지 않았다.**
valid 곡선은 −0.30 에서 정점이고 −0.50 을 쓰면 고배당 적중이 0.56% → 0.16% 로 떨어진다.
**인상적이던 25.7배는 과적합의 산물이었다.**

### 제품 계약을 위해 정확도를 내주기로 했다

축을 6개로 쪼개면 **축을 넘나드는 상호작용이 사라진다.** 실측으로 비용이 나왔다:

```
LightGBM 73피처 통짜   33.5%
6축 타워 → 순위 환산   32.9%       ← 채택
LGB 축 랭커 6개        31.0%
지표 순위 평균         28.3%       ← 백엔드의 현재 구현
```

**−0.6%p 를 내주고 "유저가 비율을 실시간으로 바꿔도 재학습이 없다"를 산다.**
이건 정확도 손해가 아니라 **교환**이고, 숫자를 알고 하는 교환이라 받아들일 수 있다.

반대로 28.3%(지표 평균)는 **아무것도 사지 않고 4.6%p 를 버리는 것**이라 고쳐야 한다.

### 모델 셋을 합치는 것에 기대를 접었다

9/11 실시간 9경주에서 **정원님 1:1:1 앙상블과 내 75피처 LGB 가 8경주에서 같은 말**을
골랐고 적중도 2/9 로 같았다. 계열 16종 상관 0.94 와 같은 그림이다.

합치는 **방법**을 고민하기 전에 **합쳐도 거의 안 바뀐다**는 게 먼저 나와 있었다.

---

## 6. 아직 모르는 것

- **천장이 왜 35% 근처인가.** 시장은 39.3% 다. 시장이 가진 정보 중 우리가 못 보는 게 무엇인지 모른다
- **2·3착 확률에 낙관 편향**이 있다 (도연님 `ai/model/README.md` §6-1). λ 보정 미적용
- **기수 변경 시 `F3_jk_*` 를 다시 못 만든다.** 프레임에 그 시점 누적 상태가 없어 경고만 띄운다
- **`ref_profit_bp`(지표별 단독 수익률)를 못 채웠다.** 확정배당이 필요한데 valid 파케이에 `winOdds` 가 없다
- 실제 서버를 띄워 `GET /races/{id}/card` 응답을 받아 본 적이 없다. 프런트 진단은 코드에서 읽은 결론이다

---

## 7. 다음 사람에게

1. **표준오차를 먼저 계산해라.** 뭘 재기 전에 "이 표본으로 얼마짜리 차이를 볼 수 있나"를 알아야 한다
2. **말로 정할 것 같은 문제를 계측해봐라.** 마체중 tier도 배당 가용성도 온도도, 재니까 끝났다
3. **"차이 없음"을 의심해라.** 코드가 안 돌아서 차이가 없는 경우가 실제로 있었다
4. **top-1 이 안 움직이면 효과 없다고 써라.** logloss만 좋아진 걸 성과로 포장하지 마라
5. **`--watch` 같은 걸 켰으면 끝을 박고 회수해라**
