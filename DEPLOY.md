# 서버에서 자동으로 돌리기

실시간 예측 파이프라인을 AI 서버(또는 상시 켜 둔 PC)에 올리는 법.
파이프라인 자체 설명은 [README.md](README.md), 왜 이렇게 됐는지는 [JOURNEY.md](JOURNEY.md).

## 0. 결론

**`raceday_cron.py` 하나만 걸면 된다.** 경마 시행일이 아니면 3콜 쓰고 스스로 끝낸다.

```bash
# 리눅스 — 경마는 금·토·일에 열린다
0 8 * * 5,6,0  cd /srv/horse && /usr/bin/python3 raceday_cron.py >> logs/cron.log 2>&1
```

## 1. 데이터는 크롤링이 아니라 **공공데이터 OpenAPI** 다

`kra_client.py` → `https://apis.data.go.kr/B551015` (한국마사회 공공데이터).
인증키는 `.env` 의 `KRA_API_KEY_ENCODED`.

> `probe_odds_live.py` 만 `m.kra.co.kr` 을 긁는데, 그건 **"실시간 배당이 웹에 있나"를
> 조사한 계측기**고 답은 "없다"였다. 예측 경로에는 크롤링이 없다.
> **운영에 이 스크립트를 올리지 마라** — 할당량을 태운 장본인이다(§4).

## 2. 서버에 있어야 하는 것

| | 크기 | 어디서 |
|---|---:|---|
| `data/raw/ledger_2010_2026.csv` | 190MB | `tools/backfill_ledger.py` 로 재생성 (몇 시간) |
| `data/raw/aux_train.csv` | 139MB | `tools/backfill_aux.py` |
| `data/raw/aux_shoe.csv` | 65MB | `tools/backfill_aux.py` |
| `data/raw/ebv_long.csv` | 24MB | `ebv_join.py` |
| `out/models/m75_*.txt` | 작음 | `raceday.py train` (1~2분) |
| `.env` | — | `KRA_API_KEY_ENCODED` |

**깃에 없다**(용량). 옮길 때는 파일째 복사하는 게 빠르다.

요구 사양 — 파이썬 3.11+, `pandas` `numpy` `lightgbm` `pyarrow`.
프레임 빌드에 메모리 **약 4GB**, 시간 **4~6분**(CPU).

## 3. 하루 흐름

```
08:00  raceday_cron.py 시작
         ① 편성 확인       3콜. 없으면 즉시 종료
         ② 원장 보강       지난 개최일 결과를 덧붙인다 (멱등)
         ③ 프레임 빌드     as-of 롤링 + 보조 조인. 4~6분
         ④ 자동 예측       각 경주 T-10분 창에서 발사. 마지막 경주까지 대기
~18:00 마지막 경주 끝나면 스스로 종료
```

`④` 는 첫 발주까지 알아서 기다리므로 아침에 한 번에 걸어도 된다.
예측은 `out/live_pred_{날짜}.csv` 에 한 줄씩 쌓인다.
경주가 끝난 뒤 `python raceday.py score --date {날짜}` 로 채점한다.

## 4. API 할당량 — 2026-09-14 사고와 대책

### 무슨 일이 있었나

9/11 에 배당 관측기를 `--watch` 로 켜고 **회수하지 않았다.** 사흘간 5분마다 돌면서:

| 날짜 | 관측 | API 호출 |
|---|---:|---:|
| 09/11 | 2,672 | 약 5,344 |
| 09/14 | 3,456 | **약 6,912** |

공공데이터포털 개발계정은 보통 **일 10,000콜**이다. 한 스크립트가 다 먹었고,
9/14 에 아무것도 못 했다. 기록에는 `T-4,429분` 짜리 관측이 남아 있다.

### 예측 파이프라인은 태우지 않는다

```
편성 확인      3콜
경주당         2콜  (출전 정보 갱신 + 당일값)
17경주 기준    약 37콜        ← 할당량의 0.4%
원장 보강      개최일당 3~6콜
```

### 넣은 대책 넷

**① 한도 초과를 재시도하지 않는다.** 전에는 HTTP 429 를 일반 오류로 보고 3번씩
재시도했고, 실패 메시지가 `호출 실패: HTTP Error 429` 뿐이라 **원인을 알 수 없었다.**
이제 `KRAQuotaError` 로 즉시 멈추고 "일일 한도 초과, 자정에 초기화" 라고 말한다.
본문 응답의 `resultCode=22` 도 같이 잡는다.

**② 프로세스별 호출 예산.** `KRA_CALL_BUDGET`(기본 1500)을 넘으면 즉시 중단한다.
어떤 버그가 나도 하루 몫을 다 먹지 못한다.

```
KRAQuotaError: 이 프로세스 호출 예산 1500 을 넘었다 (지금 1501).
               폭주를 의심해라 — 반복 루프에 종료 조건이 있는지 보라.
```

**③ 날짜별 호출 기록.** `KRA_CALL_LEDGER`(기본 `out/kra_calls.json`)에 25콜마다 누적을
남긴다. **프로세스 밖에서 "오늘 몇 콜 썼나"를 볼 수 있어야 사고를 일찍 본다.**

**④ 관측기에 끝을 박았다.** `probe_odds_live.py --watch` 는 **마지막 발주 +20분**
또는 **2,000콜**에서 멈춘다. 발주 시각을 못 읽으면 반복 자체를 안 한다.

### 그래도 모자라면

- **키를 분리해라.** 예측용과 조사용 계정을 나누면 조사가 예측을 막지 못한다
- **활용 신청으로 한도를 올릴 수 있다.** 개발계정 10,000 → 운영계정은 협의
- **원장 보강을 매일 하지 마라.** 개최일 다음 날 한 번이면 충분하다

## 5. 확인하는 법

```bash
python raceday_cron.py --dry-run          # 편성만 본다 (3콜)
python raceday_cron.py --skip-predict     # 빌드까지만
cat out/kra_calls.json                    # 날짜별 누적 호출
tail -f out/live_pred_$(date +%Y%m%d).csv # 예측이 쌓이는지
```

`--dry-run` 이 `편성 없음 — 경마 시행일이 아니다` 로 끝나면 정상이다.

## 6. 아직 안 된 것

- **모델 재학습 자동화 없음.** `raceday.py train` 을 사람이 돌려야 한다(주 1회면 충분)
- **알림 없음.** 실패해도 로그에만 남는다. 슬랙/메일 훅이 필요하다
- **기수 변경 반영 못 한다.** `F3_jk_*` 를 다시 만들 수 없어 경고만 띄운다
- **한 대에서만 돈다.** 이중화·락 없음. 두 곳에서 동시에 돌리면 호출이 두 배가 된다
