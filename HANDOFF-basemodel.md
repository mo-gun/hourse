# 인계 — 베이스 모델(6축 딥러닝 랭커) 작업 이어받기

**작성 2026-09-10, 맥북에서 작업한 것을 SSAFY 노트북으로 넘긴다.**
데이터 백필 작업 인계는 [HANDOFF.md](HANDOFF.md) 를 보라 — 이 문서는 **모델** 쪽이다.

---

## 0. 결론부터 — 무엇이 생겼나

기존 HANDOFF.md 마지막 줄이 이렇게 끝나 있었다.

> 인기도 4개만 쓴 모델이 77개 쓴 모델보다 낫다 — 펀더멘털을 얹으면 배당 정보가
> 희석된다는 뜻이라, **방향은 Benter 2단계 결합이다.**

**그 방향으로 만든 것이 이번 작업이다.** 배당을 6축 중 하나로 넣어 1/6 로 희석시키는 대신,
**7번째 타워로 빼내 프리셋마다 고정 계수 `m` 으로** 얹었다.

```
점수 = m · s_시장 + Σ w_k · s_k        Σ w_k = 100 (유저 슬라이더), m = 프리셋 상수
```

| | top-1 | top-3 | logloss |
|---|---:|---:|---:|
| 시장 (인기 1위마) | 39.47 | 69.46 | 1.7504 |
| LightGBM 77 | 39.07 | 68.58 | 1.7387 |
| 정원님 S5 타워 균등가중 (배당을 1/6 로) | 35.33 | 65.63 | 1.8656 |
| **배당형** (m=+1.86) | **37.88** | **68.82** | **1.7995** |
| 기본형 (m=0, 배당 안 봄) | 33.33 | 63.00 | 1.9480 |

- 배당형 − 타워 **+2.55%p [+0.48, +4.47] p=0.014** → 유의
- 배당형 − 시장 −1.59%p [−3.63, +0.36] p=0.29 → **차이 없음**
- 기본형 − 시장 −6.14%p p<0.001 → 유의하게 열세

**시장은 못 이긴다. 다만 배당을 쓰면 대등하고, 시장에 없는 정보는 갖고 있다**
(조건부 로짓 우도비 LR 19.11 p=1.2e−05, 무작위 대조군은 p=0.86 으로 걸러짐).
결합해도 적중률은 안 오르고 logloss 만 유의하게 좋아진다(1.7504 → 1.7412).

---

## 1. 코드가 어디 있나

**정원님 GitHub 레포의 브랜치에 있다. 이 레포에는 코드를 복사해 두지 않았다** —
두 곳에 두면 갈라진다.

- 레포: https://github.com/JeongWon4034/horse-pred-engine
- 브랜치: `feat/basemodel-axis-ranker`
- PR: https://github.com/JeongWon4034/horse-pred-engine/pull/13 (리뷰 대기)
- 폴더: `basemodel/` (최상위, `pipeline/` 과 나란히). **정원님 코드는 한 줄도 안 건드렸다**

---

## 2. SSAFY 노트북 세팅 (git bash 기준)

```bash
# 1) 코드
cd /c/Users/SSAFY/Desktop/말고리즘          # 팀 레포가 있는 폴더 옆에
git clone https://github.com/JeongWon4034/horse-pred-engine.git
cd horse-pred-engine
git checkout feat/basemodel-axis-ranker

# 2) 데이터 — 팀 레포에서 가져온다 (pipeline/data/, gitignore, 65MB)
cd pipeline
TEAM_REPO=/c/Users/SSAFY/Desktop/말고리즘/S15P21A304 bash sync_dataset.sh
cd ..

# 3) 파이썬 환경 (uv)
cd basemodel
uv sync

# 4) 확인 — 시장 39.3/69.3 이 나오면 정상
PYTHONUTF8=1 uv run python -c "import sys;sys.path.insert(0,'.');from basemodel.team import C;print(C.report({},C.load('valid')))"
```

**Windows 주의 3가지**

| | |
|---|---|
| `PYTHONUTF8=1` | **모든 명령 앞에 붙여라.** 안 붙이면 cp949 에서 한글 출력이 깨진다. 정원님 장부에도 같은 주의가 있다 |
| torch | `pyproject.toml` 은 PyPI 기본 휠이다(맥 MPS 기준). **NVIDIA GPU 가 있으면** `pipeline/pyproject.toml` 처럼 cu130 인덱스를 걸어라 — 주석에 적어 뒀다 |
| 학습 시간 | 맥 M4 에서 모델 1개 2분, `run_all.sh` 전체 25분. **노트북 CPU 면 3~4배** 잡아라. GPU 있으면 훨씬 빠르다 |

### 돌려보기

```bash
cd basemodel
PYTHONUTF8=1 uv run python -m basemodel.train --epochs 30 --tag _final              # 77피처
PYTHONUTF8=1 uv run python -m basemodel.train --no-market --epochs 30 --tag _final  # 73피처
PYTHONUTF8=1 uv run python -m basemodel.export --ckpt artifacts/runs/axis_77_s20260901_final.pt
PYTHONUTF8=1 uv run python -m basemodel.significance --ckpt artifacts/runs/axis_77_s20260901_final.pt
bash run_all.sh    # 위 전부 + 시드 5개 + 분해표 + 성능 + 허깅페이스 폴더
```

읽을 것은 `basemodel/README.md`(설계·결과)와 `basemodel/ledger.md`(실험 장부 + **실수 기록 7건**).

---

## 2-B. 이미 끝난 것 — 반복하지 마라

`99a1dae` (2026-09-08) 에서 **2004~2009 원장 백필 + A/B 검정**을 이미 했다.

- 원장 456,210 → **614,742행** (2004~2026), train 36,586 → 51,476경주
- **결론: 효과 없음.** McNemar p=0.868(77) / 1.000(73), 이원 ANOVA 에서 데이터셋 기여 0.0%(p=0.757)
- PSI 는 오히려 개선(0.132 → 0.119) — 분포 이동 가설도 기각
- **데이터 양은 병목이 아니다.** 팀 `docs/dataset` 은 그래서 2010~ 그대로 두었다

그때 나온 두 가지가 이번 모델 작업과 같은 곳을 가리킨다.

| 그때 발견 | 이번 작업의 같은 결론 |
|---|---|
| **77피처는 시장 추종자다 — 인기 1위와 89.8% 일치** | 73피처로 시장 1인기마를 맞히면 64.2% 재현. 시장에서 벗어나면 우리가 1.7배 더 틀린다 |
| 시드 3개 앙상블만으로 +0.69%p | 시드 5개 앙상블 33.43 → 33.89 (+0.46%p) |

---

## 3. 다음 할 일 — 우선순위

### ① 마체중(`X_wgHr`) 을 스키마에 등록 ★ 이 노트북에서만 할 수 있다

**원장(`data/raw/ledger_2010_2026.csv`, 2004~2026 · 614,742행)이 이 노트북에만 있어서**
맥에서는 못 했다.

- `build_v2.py:103` 이 `X_wgHr`·`X_wgHr_delta` 를 **계산은 하는데**,
  `schema_v2.py` 의 `X` 블록(10개)에 **등록이 안 돼 있어 학습셋에서 빠진다.**
  스키마 주석이 경고하는 바로 그 실수다 — "features() 가 반환하는 것만 살아남는다"
- 게임풀에서 재본 값: **우리 모델 위에 유의**(LR 15.79, p=7.1e−05), **시장 위에는 없음**(p=0.44).
  적중률 개선은 확인 안 됨(−0.26%p [−0.88, +0.36]) → **기대치를 낮게 잡아라.**
  "시장은 이미 체중을 알고 있고 우리만 모른다"가 정확한 요약이다

**tier 를 정하려면 발주 전에 알 수 있는지부터 확인해라.**
`2026-09-08` 커밋(`99a1dae`)에서 **`API25_1/entryHorseWeightInfo_1`(출전마 체중)이 이미
승인돼 있고 4개 경마장 + 2004년 소급**이라고 확인해 뒀다(`kra_client.py` 의 `entry_weight`).
그때 "API317 신청 불필요"라고 적은 것이 이것이다.

- **발주 전에 값이 차면** → `tier="B"`(발주 T-60~90분)로 등록. 스키마 `TIER` 가 이미
  마체중을 B 로 가정하고 있다. 이러면 **주말 실시간 모델(73피처)에도 쓸 수 있다**
- **경주 후에만 찬다면** → 반드시 `tier="G"`(리플레이 전용).
  A/B 로 넣으면 실시간 모델에 미래 정보가 샌다. `features(exclude_tier=("G",))` 가 자동으로 뺀다

확인 방법은 이 레포에 이미 있는 방식 그대로 — 경마 시행일(금·토·일) 발주 **전**에
`API25_1` 을 호출해 `totalCount` 와 값이 차 있는지 본다. 원장의 `wgHr` 는 경주가 끝나야
들어오므로 그것과 혼동하지 마라.

### ② "비 오면 가벼운 말" 가설 — 이미 기각됐다. 반복하지 마라

게임풀 3,063경주 실측. **모든 주로 상태에서 무거운 말이 더 이긴다.**

| 주로 | 가벼운쪽 승률 | 무거운쪽 승률 | 차이 |
|---|---:|---:|---:|
| 건조·양호 | 6.52% | 11.55% | −5.03%p |
| 다습·포화·불량 | 7.10% | 11.06% | −3.96%p |

젖으면 격차가 줄긴 하지만 차이의 차이 **+1.07%p, CI [−0.62, +2.76] → 유의하지 않다.**

다만 **모델이 상호작용을 배우는 것 자체는 확인됐다.** 입력의 선행마 수만 1두→5두로 바꾸면
추입 +0.0285 · 중단 +0.0126 · 선입 −0.0120 · **선행 −0.0359** 로 움직인다.
기획서의 "선행마 몰리면 오버페이스 → 추입 유리"를 손으로 안 넣었는데 스스로 배웠다.

### ③ 결합식 확정 — 기획·도연님과

정원님 `exp/tower-hist` §6-3 은 `점수 = 베이스(S3) + λ·Σw·s` 를 제안하고,
이 작업은 `점수 = m·s_시장 + Σw·s` 를 택했다. **진단은 같고 처방이 다르다.**
둘 다 넣을 수도 있다. 도연님이 별도 6축 파이프라인(`ai/model/`)을 갖고 있어
소유 범위도 같이 정해야 한다(정원님 §6-4).

### ④ 그 외

- **실력 축 상한** — 77피처에서 단독 21.0% 로 합격선(25) 미달. EBV 결측(2016 이전 0%)이 원인.
  `feature_group.max_weight` 로 막을 것을 BE 에 제안해 둠
- **이력 길이 L** — 정원님 L 곡선상 L=5 로 줄여도 같을 가능성이 높은데 지금 L=20. 미측정
- **test 미개봉** · **실경기 사전 예측 미실시**
- **표본** — 2%p 차이를 잡으려면 주말 13주치가 더 필요하다(검출 한계 top-1 ±2.39%p)

---

## 3-B. 2026-09-10 (SSAFY 노트북) 진행분 — 위 §3 우선순위의 현재 상태

커밋 `ae6832e` `7cec584` `e5ec17a`. **정원님 레포에는 아직 아무것도 올리지 않았다**
(요청이 있을 때만 올리기로 결정).

### ⚠ 환경이 바뀌었다 — 위 §5 붙여넣기 블록의 원장 설명은 이제 틀리다

| | |
|---|---|
| `data/raw/ledger_2010_2026.csv` | **2010~2026 · 446,031행으로 되돌렸다** (원래대로) |
| `data/raw/ledger_2004_2026.csv` | 2004~ 확장본 615,416행. gitignore. 백필은 기각됐으니 기본값이 아니다 |

되돌린 이유 — 마체중 A/B 를 재려면 기각된 백필과 섞이면 안 된다. 팀 `docs/dataset` 도
2010~ 이므로 이쪽이 기준선과 맞는다. 9월 완료분(9/4·5·6·10) 536행은 보강해 뒀다
(`tools/append_september.py`, 멱등).

### ① 마체중 — **닫았다**

- **tier 실측 확정**: 9/10 제주에서 4R 이 **T-65분 0/8 → T-61분 8/8** 로 전환.
  같은 시점 3R 은 T-36분에 이미 7/7. → **각 경주 발주 약 T-61~65분**.
  D-1 에는 없다(9/11~13 5건 423두 전부 0). `probe_wghr_raceday.py` 가 이걸 재는 도구다.
- `TIER["B"]` 의 "T-60~90분" 은 `docs/주제기획` 의 **[추정]** 이 굳은 값이었고 실측이
  아니었다. 아래쪽 끝(T-60)은 맞고 위쪽(T-90)은 근거가 없다 — T-61~65 로 정정했다.
- 원장 `wgHr` 와 `API25_1` 의 `wgHr` 는 지난 개최일 4건 365두에서 **불일치 0**.
- **A/B 결과 적중률 개선 0** (리플레이 −0.16%p p=0.864 · 실시간 −0.24%p p=0.795).
  게임풀 실측(−0.26%p)과 독립적으로 일치. → **`tier="P"` 로 비활성.** 피처 77/73 유지.
  되살리려면 tier 한 글자만 바꾸면 되고 파케이에 컬럼이 이미 있어 재빌드 불필요.

### ③ 결합식 — **확정했다** (`arena/ledger.md` 참조)

`점수 = m·s_시장 + Σ w_k·s_k` 로 간다. 근거 넷:
1. 배당을 원피처로 던지면 **gain 87.8% 가 F6** 로 가고 펀더멘털이 12%로 희석된다
2. F6 를 6축 중 하나로 두면 1/6 고정 희석 → 타워 35.33 vs 배당형 37.88 (+2.55 p=0.014)
3. 결합 계수는 **held-out 에서** 적합한다 (train 적합은 b 가 과대해진다 — 역배형 m 과 같은 함정)
4. **펀더멘털 기저는 신경망이어야 한다** — Benter 2단계를 기저 6종에 적합해 보니
   트리 기저는 fold2 에서 b 가 0 으로 무너지거나 부호가 뒤집히고(lgb +0.030 / hist −0.059),
   신경망만 두 fold 모두 양수로 살아남고(+0.295 / +0.156) 시장과 통계적 동률(p=0.126)이다.
   정원님 S3 가 logloss 에서 10/10 이긴 것과 **독립적으로 같은 결론**이다.

정원님 §6-3 의 "베이스 항" 제안과 배타적이지 않다 — 베이스 항이 `Σ w_k s_k` 이고
그 위에 시장 항을 명시적으로 얹는 형태다. **둘 다다.** (기획·도연님 소유범위 협의는 남았다)

### ④ 실경기 사전 예측 — **했다**

9/11 (제주 7 + 부경 9 = 16경주 149두)을 **발주 전에 봉인**했다
(`experiments/out/pred_20260911_20260910_150709.csv`, 2026-09-10 15:07:09).
D-1 가용분만 썼다 — 73피처 중 71개가 tier A 다. 채점은 `experiments/score_20260911.py`.

★ 그 과정에서 **같은 날 누수**를 발견해 차단했다. 예정 경기는 `ord` 가 비어 완주 필터에
걸리므로 가짜 착순을 넣어 통과시키는데, `rolling` 의 update 가 모든 행에 무조건 돌아서
**기수·조교사·부마**(하루에 여러 경주에 나온다) 피처에 앞 경주의 가짜 착순이 들어간다.
149행 중 기수 106·조교사 103·부마 70 행이 오염됐었다. 첫 등장 값 전파로 차단했고
`--leakcheck` 가 검증한다. **`predict_0905.py` 에도 같은 구조의 누수가 있다** —
거기는 착순이 진짜라 안 걸리지만 하루치를 한 번에 예측하면 뒤 경주가 앞 경주 결과를 본다.

### ⑤ 새로 생긴 것 — `arena/` (기법 계열 비교장)

⚠ **§1 의 "두 곳에 두면 갈라진다" 원칙을 이번엔 어겼다.** 정본은 정원님 레포의
`arena/` 이고 여기 있는 것은 유실 방지 사본이다. `arena/WHERE-THIS-LIVES.md` 참조.
**수정은 팀 레포 쪽에서 하고 여기로 복사하는 방향을 지킬 것.**

16개 계열(GBDT 3구현·목적함수 5종·배깅·타깃교체·신경망·PCA)을 같은 채점기로 쟀다.
**결론: 모델 계열을 바꾸는 것으로는 선을 못 넘는다.** 유의한 차이 둘 다 나쁜 쪽이고
(xgb_ndcg −1.28 p=0.040, lgb_speedfig −4.15 p<0.001), 계열 간 예측 상관 중앙값 0.935 다.
PCA 는 **정보를 안 버린 순수 회전조차** −0.72%p (트리가 축 정렬 분기를 쓰기 때문).

### ⑥ 유일하게 성과가 난 방향 — 안 쓰던 컬럼에서 정보 뽑기

원장 89컬럼 중 피처로 한 번도 안 쓰인 것에서 만들었다. **새 수집 0건.**

| | logloss | 차이 | Wilcoxon p |
|---|---:|---:|---:|
| 기준선 73피처 | 1.9031 | — | — |
| H1 착차(마신차) 2개 | 1.8939 | −0.0091 | **0.00018** |
| H2 경쟁강도 2개 | 1.8938 | −0.0093 | **0.00191** |
| **H1+H2** | **1.8902** | **−0.0128** | **0.00105** |

top-1 은 셋 다 변화 없다(p=1.000 / 0.652 / 0.788). Bonferroni 통과, 효과 거의 가법적.
구성 과정에서 실측으로 바로잡은 것 — **착차는 우승마 누적이 아니라 앞말 대비**(기록차와
r=0.9932로 교차검증) · **`diffUnit=500` 은 코드값**(실제 32.6초≈185마신) ·
**`race_id` 키를 문자열 meet 로 만들어 교집합 0** 이 된 사고 ·
`prizeCond` 는 246개 이질표기라 텍스트 파싱 대신 원장에서 상대 실력을 직접 집계.

### 다섯 번 반복된 패턴 — 이게 이 문제의 구조다

| 추가한 정보 | 유의성 | top-1 |
|---|---|---|
| 마체중 | LR 15.79 p=7.1e−05 | 변화 없음 |
| Benter 2단계 결합 | logloss 유의 개선 | 변화 없음 |
| 정원 S3 전적 GRU | logloss 시드 10/10 우위 | 변화 없음 |
| H1 착차 | logloss p=0.0002 | 변화 없음 |
| H2 경쟁강도 | logloss p=0.0019 | 변화 없음 |

**top-1 로만 값을 재면 유일하게 성과가 난 방향을 놓친다.** 제품이 보여주는 건 승리
확률이므로(`softmax(znorm(raw)×T)`) logloss 개선은 화면에 직접 들어간다.

### 남은 것

- **test 미개봉** (802경주). H1+H2 효과를 잡으려면 경주 834개가 필요한데 test 가 802 라
  **경계선(검정력 ~78%)** 이다. 지금 H1+H2 하나에 쓰지 말고 전부 확정된 최종 구성으로
  한 번에 열 것. valid 는 오늘 약 25번 측정해 이미 닳았다
- **train 내부 시간분할 교차검증** — valid·test 를 안 건드리고 검정력을 30배 얻는 길. 미실시
- 이력 길이 L 미측정 · 실력 축 상한 BE 제안 상태 · 기획·도연님 소유범위 협의

---

## 4. 주의 — 이 작업에서 실제로 밟은 지뢰

전문은 `basemodel/ledger.md` §실수 기록. 특히 셋은 다시 만날 수 있다.

1. **시장 축을 학습할 때 계수를 음수까지 샘플링하면 타워가 망가진다.** "1착마 점수를
   올려라"와 "내려라"가 상쇄되어 배울 방향이 없어진다. top-1 이 5.7%(무작위 10.2% 미만)까지
   떨어졌다. 부호 뒤집기는 **프리셋의 몫**이지 학습의 몫이 아니다
2. **시장을 손실에 넣으면 나머지 축이 무임승차한다.** 실력 축 단독이 8.0% 까지 떨어졌다.
   손실을 둘로(`PL(m=0) + PL(m~U)`) 나눠야 회복된다
3. **시장 확률을 점수로 그냥 쓰면 안 된다.** softmax 가 한 번 더 걸려 거의 균등이 된다.
   그렇게 재면 시장 logloss 가 2.21(무작위 2.38 근처)로 나온다. `log(확률)` 을 써라

---

## 5. 새 Claude 세션에 붙여넣을 것

아래 블록을 통째로 복붙하면 된다.

```
경마 예측 프로젝트 모델 작업을 이어받는다. 맥북에서 하던 걸 이 노트북으로 옮겼다.

## 지금 상태

베이스 모델(6축 딥러닝 랭커)을 만들어 정원님 GitHub 에 PR 을 올려둔 상태다.
  레포     https://github.com/JeongWon4034/horse-pred-engine
  브랜치   feat/basemodel-axis-ranker
  PR       #13 (리뷰 대기)
  폴더     basemodel/  ← 여기가 내 작업. pipeline/ 은 정원님 것이니 건드리지 마라

## 먼저 읽어라

1. basemodel/README.md   설계와 결과 전부
2. basemodel/ledger.md   실험 장부 + 실수 기록 7건 ★ 같은 실수 반복 방지
3. (이 레포) HANDOFF-basemodel.md   이 문서
4. (이 레포) HANDOFF.md   데이터 백필 쪽 인계

## 환경

- 이 노트북 개인 폴더: C:\Users\SSAFY\Desktop\주제선정
    data/raw/ledger_2010_2026.csv  ★ 이 노트북에만 있다 (파일명은 그대로지만 2004~2026, 614,742행)
- 팀 레포: C:\Users\SSAFY\Desktop\말고리즘\S15P21A304 (GitLab, develop)
- 모델 레포는 위 GitHub. 데이터는 pipeline/sync_dataset.sh 로 팀 레포에서 가져온다

세팅:
    cd /c/Users/SSAFY/Desktop/말고리즘
    git clone https://github.com/JeongWon4034/horse-pred-engine.git
    cd horse-pred-engine && git checkout feat/basemodel-axis-ranker
    cd pipeline && TEAM_REPO=/c/Users/SSAFY/Desktop/말고리즘/S15P21A304 bash sync_dataset.sh
    cd ../basemodel && uv sync

★ 모든 python 명령 앞에 PYTHONUTF8=1 을 붙여라. 안 붙이면 한글 출력이 깨진다.
★ NVIDIA GPU 가 있으면 basemodel/pyproject.toml 주석대로 cu130 인덱스를 걸어라.

## 첫 작업 — 마체중(X_wgHr) 스키마 등록

build_v2.py:103 이 X_wgHr·X_wgHr_delta 를 계산하는데 schema_v2.py 의 X 블록에
등록이 안 돼 학습셋에서 빠져 있다. 원장이 이 노트북에만 있어 여기서만 할 수 있다.

먼저 tier 를 정해라 — 발주 전에 알 수 있는 값인지가 갈림길이다.
  kra_client.py 의 entry_weight = API25_1/entryHorseWeightInfo_1 (출전마 체중).
  2026-09-08 커밋에서 "이미 승인, 4개 경마장 + 2004년 소급" 으로 확인해 뒀다.
  경마 시행일 발주 '전'에 호출해 값이 차는지 보라.
  - 차면   -> tier="B" 로 등록. 주말 실시간 모델(73피처)에도 쓸 수 있다
  - 안 차면 -> tier="G" (리플레이 전용). A/B 로 넣으면 실시간 모델에 미래가 샌다
  원장의 wgHr 는 경주가 끝나야 들어오는 값이니 혼동하지 마라.

등록 후:
  1. python build_v2.py all -> validate_v2.py 전 항목 통과 확인
  2. 팀 docs/dataset 에 반영 (별도 브랜치 + MR, develop 직접 푸시 금지)
  3. basemodel 재학습 후 significance.py 로 시장 대비 재측정

기대치는 낮게 잡아라 — 게임풀에서 미리 재봤더니 우리 모델 위에는 유의하지만
(LR 15.79, p=7.1e-05) 적중률 개선은 확인 안 됐다(-0.26%p, CI [-0.88, +0.36]).
"시장은 이미 체중을 알고 있고 우리만 모른다" 가 정확한 요약이다.

## 하지 마라 — 이미 기각된 것

- 2004~2009 원장 백필: 이미 했고 효과 없음(99a1dae). McNemar p=0.868, ANOVA 데이터셋 기여 0.0%
- "비 오면 가벼운 말이 유리": 게임풀 3,063경주에서 기각. 차이의 차이 +1.07%p CI [-0.62, +2.76]

## 규칙

- game 은 학습·검증·튜닝 금지. test 는 열지 않는다. 비교는 valid
- 정렬(race_id 연속 블록)을 바꾸지 마라
- 시드 3개 이상 돌려 평균±표준편차로 읽어라. top-1 검출 한계가 ±2.39%p 다
- 팀 채점기(common.py)를 거치지 않고 데이터를 직접 읽지 마라
- 정원님 pipeline/ 코드는 수정하지 마라. 필요하면 basemodel/ 안에서 해라
```

---

## 3-C. 2026-09-14~15 진행분 — 모델에서 **제품 연동**으로 넘어갔다

§3 의 우선순위 ①②③④ 는 §3-B 에서 닫혔다. 이 이틀은 다른 축의 일이다 —
만든 모델을 팀 제품(front/back/DB)에 어떻게 붙이느냐.

문서 두 개가 정원님 레포 `basemodel/` 에 있다 (브랜치 `feat/basemodel-axis-ranker`):
- **`INTEGRATION.md`** — 지금 끊겨 있는 네 곳의 진단
- **`AI-CONTRACT.md`** — 백/프런트가 그대로 구현할 실행 명세

### 확정한 계약

```
점수  raw = Σ( groupWeights[k] × ax_k ) / 100        k = 6축
확률  p   = softmax( raw × 0.0445 ),  경주 안에서
적재  entry_feature_score 41칸 = ax_* 6(계산) + 표시 지표 35(화면)
```

### 온도 0.0445 — 명세값이 맞았다. 다섯 번 독립으로 확인

명세(`BotConfigDefaults`)는 `0.0445`, 백엔드 구현(`PredictionService:57`)은 `exp(s/10)`
= `0.1` 이었다. **숫자 싸움이 아니라 "점수를 어떻게 만드는지"를 안 정한 문제였다.**
`schema.sql:307` 의 "0~100. 경주 내 순위 기반 환산" 한 줄이 세 가지로 읽히고,
읽기마다 최적 T 가 0.037 / 0.041 / 0.069 로 달랐다.

축 점수를 **경주 내 순위로 환산**하면(②) 점수 폭이 모델과 무관하게 고정돼 T 가 안 흔들린다:

| 축 점수 생성기 | 최적 T |
|---|---:|
| 지표 순위 평균 / 대표 지표 1개 / 순위 중앙값 / z평균 | 0.0408 / 0.0447 / 0.0444 / 0.0406 |
| LGB 축 랭커 6개 | 0.0411 |
| 6축 타워 (채택) | 0.0478 |

타워 내부의 `T=1.436`(z 척도)과 같은 온도다 — `1.436 / 28.7 ≈ 0.050`.
**`0.1` 만 어느 자에도 안 맞는다.**

### 축 점수는 모델이 낸다 — 지표 평균은 4.6%p 를 버린다

| 축 점수를 만드는 법 | top-1 | top-3 |
|---|---:|---:|
| 지표값 순위 환산 → 그룹 평균 *(현재 BE 구현)* | 28.3% | 56.9% |
| LGB 축 랭커 6개 | 31.0% | 60.5% |
| **6축 타워 → 순위 환산 (채택)** | **32.9%** | 62.7% |
| *(상한) LGB 73피처 통짜* | *33.5%* | *63.3%* |

valid 1,254경주 · top-1 표준오차 ±0.85%p.

### 산출물 (mo-gun/hourse)

```
tools/fit_temperature*.py     온도 측정 5종 — 계약·도달가능영역·민감도·강건성
tools/build_feature_master.py 지표 마스터 (축·방향·가용시점·단독적중률)
tools/emit_feature_seed.py    → out/feature_seed.sql  백엔드가 그대로 INSERT
tools/build_axis_scores.py    LGB 축 랭커 6개 (비교 기준선)
tools/roundtrip_tower.py      타워 축 점수 왕복 검증
tools/emit_score_table.py   ★ 적재기 — POST /ai/score-tables 형식 + 파일 재읽기 검증
tools/score_weekend.py        블라인드 합산 채점 (윌슨 구간 · 갈린 경주만)
```

체크포인트는 `basemodel/artifacts/runs/axis_73_s20260901_final.pt` 로 학습해 뒀다.
**CPU 에서 33분** 걸린다 (README §9 의 "약 2분" 은 GPU 기준이다).

### 팀이 고칠 곳 셋 — 남의 코드라 문서로만 넘겼다

| 어디 | 무엇 | 근거 |
|---|---|---|
| BE `PredictionService:57` | `exp(s/10.0)` → `exp(s*0.0445)` | logloss +0.40 |
| BE `EntryFeatureScoreAxisProvider` | 그룹 평균 대신 `ax_*` 6행을 읽는다 | top-1 −4.6%p |
| FE `rules.js deckScores` | `groupWeights` + softmax, min-max 재척도 제거 | 점수가 전부 `-` 로 나온다 |

### 보류한 것 — 근거가 약하다

`pae_feature_weight`(그룹 안 지표 비중) 적합은 logloss 를 −0.0167 개선하지만
top-1 은 +0.6%p 로 표준오차(±1.27%p) 안이다. **이 프로젝트에서 여섯 번째로 같은 패턴**이다
(마체중·Benter 결합·S3 GRU·H1·H2·이번) — 정보는 늘고 1등 고르기는 안 움직인다.
축 점수를 타워가 직접 주므로 그 테이블이 비어 있어도 동작한다.

### 실수 기록 — 되풀이하지 않으려고 적는다

- **비중 적합이 최적화가 아니라 버그였다.** 첫 결과가 균등값과 소수 둘째 자리까지 같았다.
  scipy Nelder-Mead 를 0 벡터에서 시작하면 초기 심플렉스가 0.00025 로만 움직여 제자리에 머문다.
  초기 심플렉스를 직접 줘서 고쳤다
- **왕복 검증을 도달 불가능한 가중치로 쟀다.** 정확 균등(16.67)은 정수·합100 제약상 만들 수
  없는데 그 값으로 재서 파일/메모리가 0.6%p 어긋나 보였다. 그 지점에서만 1위 동점이 3.3%
  경주로 뛴다. 도달 가능한 기본형에서는 0.6% 이고 파일/메모리가 정확히 일치한다
- **`--watch` 에 끝이 없었다.** 9/11 에 켜 둔 `probe_odds_live.py --watch` 둘을 회수하지 않아
  사흘간 돌며 API 할당량을 태웠다(T-4,429분 관측이 남았다). 종료 조건을 박았다
